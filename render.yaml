services:
  - type: web
    name: crypto-ai-trader
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python local_app.py
    healthCheckPath: /api/status
    envVars:
      - key: HOST
        value: 0.0.0.0
